{% extends "EventPlanners/base.html" %}

{% block content %}
	{% for cat in cats %}
		 <div>
			<h1>{{ cat.caterer_name }}</a></h1>
			<p>Cuisine:{{ cat.cuisine }}</p>
			<p>{{ cat.cost }}</p>
		</div>
	{% endfor %}
{% endblock %}
